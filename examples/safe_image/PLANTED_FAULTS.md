# safe_image

No fault is planted. Seeds are unique per image, so nothing here is a duplicate or a near-duplicate by construction; anything DD003/DD004 reports would be a false positive of the perceptual hash.

Regenerate: `python examples/build.py`
