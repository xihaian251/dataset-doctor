# safe_tabular

No fault is planted here. This is the control fixture: every finding it produces is a false positive, and the CI gate fails if a critical or blocking one appears.

Regenerate: `python examples/build.py`
