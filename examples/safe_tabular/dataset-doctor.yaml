# Generated by `dataset-doctor demo`. Declaring these fields is what lets the
# entity and temporal rules measure anything at all.
dataset:
  type: tabular
labels:
  column: target
groups:
  columns: [patient_id]
id_columns: [record_id]
policies: {}
